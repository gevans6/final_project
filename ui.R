library(shiny)
library(plotly)
library(dplyr)

shinyUI(fluidPage('Title',
                  # Create title
                  titlePanel('Title'),
                  # Create sidebar and sidepanel controls
                  sidebarLayout(
                    sidebarPanel(
                     
                    ),
                    
                    # Main panel for displaying plotly map
                    mainPanel(
                      
                    )
                  )
                  
))